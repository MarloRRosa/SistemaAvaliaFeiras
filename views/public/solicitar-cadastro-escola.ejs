<div class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">
        
        <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Solicitar Cadastro da Sua Escola</h2>

        <% if (typeof success_msg !== 'undefined' && success_msg) { %>
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg relative mb-4" role="alert">
                <span class="block sm:inline"><%- success_msg %></span>
            </div>
        <% } %>
        <% if (typeof error_msg !== 'undefined' && error_msg) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg relative mb-4" role="alert">
                <span class="block sm:inline"><%- error_msg %></span>
            </div>
        <% } %>

        <p class="text-gray-600 text-center mb-6">Preencha o formulário para que sua escola seja cadastrada. O Super Admin revisará sua solicitação.</p>

        <form action="/solicitar-cadastro-escola" method="POST" class="space-y-6">
            
            <div>
                <label for="nomeEscola" class="block text-sm font-medium text-gray-700 mb-1">Nome da Escola</label>
                <input 
                    type="text" 
                    id="nomeEscola"
                    name="nomeEscola" 
                    placeholder="Nome completo da escola" 
                    value="<%= typeof nomeEscola != 'undefined' ? nomeEscola : '' %>"
                    class="w-full p-3 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" 
                    required>
            </div>
            
            <div>
                <label for="emailAdmin" class="block text-sm font-medium text-gray-700 mb-1">Seu E-mail (Administrador da Escola)</label>
                <input 
                    type="email" <% // Use type="email" para validação básica do navegador %>
                    id="emailAdmin"
                    name="emailAdmin" 
                    placeholder="seu.email@escola.com.br" 
                    value="<%= typeof emailAdmin != 'undefined' ? emailAdmin : '' %>"
                    class="w-full p-3 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" 
                    required>
            </div>
            
            <button 
                type="submit" 
                class="w-full bg-blue-600 text-white font-semibold py-3 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200">
                Enviar Solicitação
            </button>
        </form>

        <div class="text-center mt-6">
            <a href="/" class="text-sm text-blue-600 hover:text-blue-800 hover:underline">
                ← Voltar para a página inicial
            </a>
        </div>

    </div>
</div>