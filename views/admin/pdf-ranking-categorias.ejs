<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= titulo %></title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      margin: 25px;
      color: #333;
    }

    h1 {
      font-size: 20px;
      color: #0056b3;
      text-align: center;
      margin-bottom: 25px;
      border-bottom: 2px solid #0056b3;
      padding-bottom: 10px;
    }

    .categoria-section {
      margin-bottom: 30px;
      padding: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
      background-color: #f9f9f9;
      box-shadow: 2px 2px 5px rgba(0,0,0,0.05);
      page-break-inside: avoid;
    }

    .categoria-header {
      font-size: 16px;
      color: #007bff;
      margin-top: 0;
      margin-bottom: 15px;
      border-bottom: 1px solid #e0e0e0;
      padding-bottom: 5px;
    }

    .ranking-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
      font-size: 10px;
    }

    .ranking-table th,
    .ranking-table td {
      border: 1px solid #e0e0e0;
      padding: 6px 8px;
      text-align: center;
    }

    .ranking-table thead th {
      background-color: #e6f2ff;
      color: #0056b3;
      font-weight: bold;
      font-size: 11px;
    }

    .ranking-table tbody tr:nth-child(even) {
      background-color: #f6f6f6;
    }

    .ranking-table th:nth-child(2),
    .ranking-table td:nth-child(2) {
        text-align: left; /* Nome do projeto alinhado à esquerda */
    }
  </style>
</head>
<body>
  <h1><%= titulo %> - <%= nomeFeira %></h1>
  <h3 style="text-align:center; margin-bottom:20px;"><%= escola.nome %></h3>

  <% Object.keys(rankingPorCategoria).forEach(categoria => { %>
    <div class="categoria-section">
      <h2 class="categoria-header"><%= categoria %></h2>
      <table class="ranking-table">
        <thead>
          <tr>
            <th>Posição</th>
            <th>Projeto</th>
            <th>Média Final</th>
            <th>Nº Avaliações</th>
          </tr>
        </thead>
        <tbody>
          <% rankingPorCategoria[categoria].forEach((proj, index) => { %>
            <tr>
              <td><%= index + 1 %>º</td>
              <td><%= proj.titulo %></td>
              <td><%= proj.notaFinal %></td>
              <td><%= proj.numAvaliacoes %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% }) %>
</body>
</html>
