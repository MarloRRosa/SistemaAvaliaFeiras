<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= titulo %></title>
  <style>
    /* Estilos Gerais do Corpo e Fonte */
    body {
      font-family: 'Arial', sans-serif;
      font-size: 11px; /* Tamanho de fonte um pouco menor para PDFs */
      margin: 25px; /* Margens maiores para o PDF */
      color: #333;
    }

    /* Estilos do Cabeçalho Principal */
    h1 {
      font-size: 20px;
      color: #0056b3; /* Um tom de azul para o título principal */
      text-align: center;
      margin-bottom: 25px;
      border-bottom: 2px solid #0056b3;
      padding-bottom: 10px;
    }

    /* Estilos dos Avaliadores */
    .avaliador-section {
      margin-bottom: 30px; /* Espaço maior entre os blocos de avaliadores */
      padding: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
      background-color: #f9f9f9;
      box-shadow: 2px 2px 5px rgba(0,0,0,0.05); /* Sutil sombra para destacar */
      /* --- ADIÇÃO AQUI PARA QUEBRA DE PÁGINA --- */
      page-break-inside: avoid;
    }

    .avaliador-header {
      font-size: 16px;
      color: #007bff; /* Azul um pouco mais claro para o nome do avaliador */
      margin-top: 0;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .avaliador-status {
      font-size: 11px;
      font-weight: normal;
      background-color: #e9ecef;
      padding: 3px 8px;
      border-radius: 12px;
      color: #555;
    }

    /* Estilos dos Detalhes do Avaliador (PIN e Totais) */
    .avaliador-details {
      margin-top: -5px; /* Aproxima os detalhes do cabeçalho */
      margin-bottom: 15px;
      line-height: 1.6;
    }

    .avaliador-details strong {
      color: #000;
    }

    /* Estilos da Tabela de Projetos */
    .projetos-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }

    .projetos-table th,
    .projetos-table td {
      border: 1px solid #e0e0e0;
      padding: 8px 10px;
      text-align: left;
    }

    .projetos-table thead th {
      background-color: #e6f2ff; /* Fundo azul claro para o cabeçalho da tabela */
      color: #0056b3;
      font-weight: bold;
      font-size: 12px;
    }

    .projetos-table tbody tr:nth-child(even) {
      background-color: #f6f6f6; /* Listras zebradas para facilitar a leitura */
    }

    .projetos-table td:nth-child(1) { /* Coluna Título */
        width: 70%;
    }

    .projetos-table td:nth-child(2) { /* Coluna Status */
        width: 30%;
        white-space: nowrap; /* Evita quebra de linha em "✅ Avaliado" */
    }

    /* Estilos para "Nenhum projeto atribuído" */
    .no-projects {
      font-style: italic;
      color: #777;
      margin-top: 10px;
      text-align: center;
      padding: 10px;
      background-color: #eee;
      border-radius: 4px;
    }

    /* Estilos para os ícones de status */
    .status-avaliado { color: #28a745; font-weight: bold; } /* Verde para avaliado */
    .status-pendente { color: #dc3545; font-weight: bold; } /* Vermelho para pendente */
  </style>
</head>
<body>
  <h1><%= titulo %></h1>

  <% avaliadores.forEach(av => { %>
    <div class="avaliador-section">
      <h2 class="avaliador-header">
        <%= av.nome %> (<%= av.email %>)
        <span class="avaliador-status"><%= av.ativo ? 'Ativo' : 'Inativo' %></span>
      </h2>
      <div class="avaliador-details">
        <p>PIN: <strong><%= av.pinAtivo || 'N/A' %></strong></p>
        <p>
          Projetos Atribuídos: <strong><%= av.totalAtribuidos %></strong> |
          Avaliados: <strong><%= av.totalAvaliados %></strong> |
          Pendentes: <strong><%= av.totalAtribuidos - av.totalAvaliados %></strong>
        </p>
      </div>

      <% if (av.projetos.length > 0) { %>
        <table class="projetos-table">
          <thead>
            <tr>
              <th>Título do Projeto</th>
              <th>Status</th>
            </tr>
          </thead>
          <tbody>
            <% av.projetos.forEach(p => { %>
              <tr>
                <td><%= p.titulo %></td>
                <td>
                  <span class="<%= p.status.includes('Avaliado') ? 'status-avaliado' : 'status-pendente' %>">
                    <%= p.status %>
                  </span>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <p class="no-projects"><em>Nenhum projeto atribuído.</em></p>
      <% } %>
    </div>
  <% }) %>
</body>
</html>