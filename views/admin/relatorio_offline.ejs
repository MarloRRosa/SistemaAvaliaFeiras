<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatório de Avaliação Offline - <%= feira.nome %></title>
    <style>
        body {
      font-family: 'Arial', sans-serif;
      font-size: 11px; /* Tamanho de fonte um pouco menor para PDFs */
      margin: 25px; /* Margens maiores para o PDF */
      color: #333;
    }
        header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #ccc;
            padding-bottom: 10px;
        }
        h1, h2, h3 {
            color: #2c3e50;
            margin-top: 0;
            margin-bottom: 10px;
        }
        .section {
            margin-bottom: 25px;
            padding: 15px;
            border: 1px solid #eee;
            border-radius: 8px;
            background-color: #f9f9f9;
            page-break-inside: avoid;
        }
        .project-details {
            margin-bottom: 15px;
        }
        .criteria-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        .criteria-table th, .criteria-table td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
            vertical-align: top;
        }
        .criteria-table th {
            background-color: #e0e0e0;
            font-weight: bold;
        }
        .input-box {
            border: 1px dashed #999; /* Borda tracejada para indicar campo a ser preenchido */
            min-height: 25px;
            padding: 5px;
            margin-top: 5px;
            background-color: #fff;
        }
        .page-break {
            page-break-before: always;
        }
        .avaliador-info {
            text-align: right;
            font-style: italic;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <header>
        <h1>Relatório de Avaliação Offline</h1>
        <h2>Feira: <%= feira.nome %></h2>
        <p>Período: <%= formatarData(feira.inicioFeira) %> a <%= formatarData(feira.fimFeira) %></p>
        <% if (avaliador) { %>
            <div class="avaliador-info">Relatório para Avaliador: **<%= avaliador.nome %>**</div>
        <% } %>
    </header>

    <% if (projetos.length === 0) { %>
        <div class="section">
            <p>Nenhum projeto encontrado para esta feira<%= avaliador ? ' e avaliador.' : '.' %></p>
        </div>
    <% } else { %>
        <% projetos.forEach((projeto, index) => { %>
            <div class="section">
                <h3>Projeto: <%= projeto.titulo %> (<%= projeto.numero || 'N/A' %>)</h3>
                <div class="project-details">
                    <p><strong>Escola:</strong> <%= projeto.escolaNome || 'N/A' %></p>
                    <p><strong>Alunos:</strong> <%= projeto.alunos && projeto.alunos.length > 0 ? projeto.alunos.map(aluno => aluno.nome).join(', ') : 'N/A' %></p>
                    <p><strong>Área:</strong> <%= projeto.area || 'N/A' %></p>
                    <p><strong>Categoria:</strong> <%= projeto.categoria ? projeto.categoria.nome : 'N/A' %></p>
                    <p><strong>Resumo:</strong> <%= projeto.resumo || 'N/A' %></p>
                </div>

                <h4>Critérios de Avaliação:</h4>
                <% if (projeto.criteriosAvaliacao && projeto.criteriosAvaliacao.length > 0) { %>
                    <table class="criteria-table">
                        <thead>
                            <tr>
                                <th style="width: 60%;">Critério</th>
                                <th style="width: 15%;">Nota (0-10)</th>
                                <th style="width: 25%;">Observações</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% projeto.criteriosAvaliacao.forEach(criterio => { %>
                                <tr>
                                    <td><%= criterio.nome %>
                                        <% if (criterio.descricao) { %>
                                            <br><small><%= criterio.descricao %></small>
                                        <% } %>
                                    </td>
                                    <td><div class="input-box"></div></td>
                                    <td><div class="input-box" style="min-height: 50px;"></div></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p>Nenhum critério de avaliação encontrado para a categoria deste projeto.</p>
                <% } %>

                <h4 style="margin-top: 20px;">Notas Finais:</h4>
                <p><strong>Nota Geral do Projeto:</strong> <div class="input-box" style="width: 100px;"></div></p>

                <h4 style="margin-top: 20px;">Comentários Gerais do Avaliador:</h4>
                <div class="input-box" style="min-height: 100px;"></div>
            </div>
            <% if (index < projetos.length - 1) { %>
                <div class="page-break"></div>
            <% } %>
        <% }) %>
    <% } %>
</body>
</html>