<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= titulo || 'Sistema AvaliaFeiras' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css" crossorigin="anonymous" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
            display: flex; /* Para flexbox para o layout sticky footer */
            flex-direction: column; /* Para empilhar o header, main e footer */
            min-height: 100vh; /* Garante que o body ocupe a altura total da viewport */
        }
        .hover-scale {
            transition: transform 0.3s ease-in-out;
        }
        .hover-scale:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800">

    <!-- Cabeçalho institucional -->
    

    <main class="pt-24 max-w-6xl mx-auto p-6 flex-grow"> <!-- Adicionado flex-grow para empurrar o footer para baixo -->
        <% if (success_msg) { %>
            <div class="bg-green-200 p-2 mb-4 rounded"><%= success_msg %></div>
        <% } %>
        <% if (error_msg) { %>
            <div class="bg-red-200 p-2 mb-4 rounded"><%= error_msg %></div>
        <% } %>
        <%- body %> <!-- Conteúdo da view específica será injetado aqui -->
    </main>

    <!-- Footer movido para o layout para ser consistente em todas as páginas -->
   

</body>
</html>
