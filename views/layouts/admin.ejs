<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* Estilos comuns para o layout, se houver */
        /* Estilo para a barra de rolagem dos checkboxes de projetos, se necessário */
        #projetosCheckboxesContainer, #avaliadoresProjetoCheckboxesContainer {
            max-height: 200px; /* Altura máxima */
            overflow-y: auto;  /* Adiciona rolagem vertical */
            border: 1px solid #e2e8f0; /* Borda para visualizar a área de rolagem */
            padding: 8px;
            border-radius: 0.375rem;
        }
        /* Estilo para o modal de tela cheia */
        .modal-full {
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
        }
    </style>
</head>
<body class="bg-gray-100 flex h-screen">

    <aside class="w-64 bg-gray-900 text-white p-6 pt-6 fixed top-0 bottom-0 left-0 z-30 shadow-lg">
        <nav>
            <ul>
                <li class="mb-4">
                    <a href="/admin/dashboard" class="flex items-center py-2.5 px-4 rounded transition duration-200 hover:bg-gray-700">
                        <i class="fas fa-tachometer-alt mr-3"></i> Home
                    </a>
                </li>

                <li class="mb-4">
                    <a href="/admin/dashboard-geral" class="flex items-center px-4 py-2 text-sm text-white hover:bg-blue-700">
                        <i class="fas fa-chart-bar mr-2"></i> Dashboard Geral
                    </a>
                </li>

                <li class="mb-4">
                    <a href="/admin/avaliacoes/export-pdf" class="flex items-center px-4 py-2 text-sm text-white hover:bg-blue-700" target="_blank">
                        <i class="fas fa-file-pdf mr-2"></i> Relatório em PDF
                    </a>
                </li>
            </ul>
        </nav>
    </aside>

    <main class="flex-1 p-6 ml-64"> <%- body %> </main>

</body>
</html>